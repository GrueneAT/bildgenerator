# Issue system configuration
# Generated: 2026-04-03

model: opus

source:
  type: github
  repo: GrueneAT/bildgenerator

branches:
  template: "feature/{source_id}-{slug}"

commits:
  format: conventional

repos:
  - path: .
    name: bildgenerator
