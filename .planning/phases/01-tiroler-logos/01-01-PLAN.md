---
phase: 01-tiroler-logos
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/images/logos/index.json
autonomous: true
requirements:
  - BEZ-01
  - BEZ-02
  - BEZ-03
  - BEZ-04
  - BEZ-05
  - BEZ-06
  - BEZ-07
  - BEZ-08
  - GEM-01
  - GEM-02
  - GEM-03
  - GEM-04
  - GEM-05
  - GEM-06
  - GEM-07
  - GEM-08
  - GEM-09
  - GEM-10
  - GEM-11
  - GEM-12
  - GEM-13
  - GEM-14
  - GEM-15
  - GEM-16
  - GEM-17
  - GEM-18
  - GEM-19
  - GEM-20
  - GEM-21
  - GEM-22
  - GEM-23
  - GEM-24
  - GEM-25
  - GEM-26
  - GEM-27
  - GEM-28
  - GEM-29
  - GEM-30
  - GEM-31
  - GEM-32
  - GEM-33
  - GEM-34
  - GEM-35
  - GEM-36
  - GEM-37
  - GEM-38
  - GEM-39
  - GEM-40
  - GEM-41
  - GEM-42
  - GEM-43
  - ORG-01
  - ORG-02
  - ORG-03
  - ORG-04
  - ORG-05
  - ORG-06
  - ORG-07
  - SORT-01
  - SORT-02
  - SORT-03
  - SORT-04
  - QUAL-01
  - QUAL-02
  - QUAL-03

must_haves:
  truths:
    - "A user searching for any of the 8 Tiroler Bezirke names (e.g. BEZIRK IMST, BEZIRK INNSBRUCK-LAND, BEZIRK KITZBÜHEL, BEZIRK KUFSTEIN, BEZIRK LANDECK, BEZIRK LIENZ, BEZIRK REUTTE, BEZIRK SCHWAZ) finds it in the dropdown"
    - "A user searching for any of the 43 Tiroler Gemeinden (e.g. ABSAM, HALL IN TIROL, HOPFGARTEN IM% BRIXENTAL, STADT INNSBRUCK, WÖRGL, ZILLERTAL) finds it in the dropdown"
    - "A user searching for any of the 7 Organisationen (GRÜNE FRAUEN, GRÜNE JUGEND, GRAS, GRÜNE GENERATION% PLUS, GRÜNE WIRTSCHAFT, GRÜNE BÄUERINNEN% UND BAUERN, GRÜNE IN DER AK) finds it in the dropdown"
    - "All entries within the Bezirke, Gemeinden, Andere, and Bundesländer categories appear in strict alphabetical order"
    - "No duplicate entries exist anywhere in index.json (the existing 'Innsbruck' in Gemeinden and the new 'STADT INNSBRUCK' are distinct entries — both are valid)"
    - "Running `python3 embed_logos.py index.html /tmp/out.html` exits with code 0 and prints 'Successfully embedded'"
  artifacts:
    - path: "resources/images/logos/index.json"
      provides: "Complete logo index with all Tiroler Bezirke, Gemeinden, and Organisationen, alphabetically sorted"
      contains: "BEZIRK IMST"
  key_links:
    - from: "resources/images/logos/index.json"
      to: "embed_logos.py"
      via: "json.load()"
      pattern: "valid JSON, no trailing commas, no duplicate keys"
---

<objective>
Update `resources/images/logos/index.json` to add 8 Tiroler Bezirke, 43 Tiroler Gemeinden, and 7 Teil-/Netzwerkorganisationen, then alphabetically sort all categories and verify the production build.

Purpose: GRÜNE Tirol and its member organisations can select their logos from the searchable dropdown.
Output: Updated `resources/images/logos/index.json` that passes `embed_logos.py` without error.
</objective>

<execution_context>
@/opt/claude-config/get-shit-done/workflows/execute-plan.md
@/opt/claude-config/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@resources/images/logos/index.json

## Current index.json state (as of planning, 2026-02-26)

The file has 6 top-level categories:
- `Bundesländer` — 8 entries (unsorted: e.g. "SALZBURG" before "10. BUNDESLAND")
- `Klubs` — 1 entry (leave as-is, no sorting required by requirements)
- `Bezirke` — 40 entries (Burgenland, Salzburg, Steiermark, Oberösterreich Bezirke — NOT sorted)
- `Gebiete` — 3 entries (leave as-is, not in sort requirements)
- `Gemeinden` — ~381 entries (NOT sorted; "Innsbruck" appears at line 240 — this is a DIFFERENT entry from "STADT INNSBRUCK" to be added for GEM-32)
- `Andere` — 20 entries (NOT sorted)

## Known duplicate risks (QUAL-01)

The existing entry `"Innsbruck"` (line 240, mixed-case, no prefix) represents the existing Innsbruck entry (likely a Vorarlberg-region or standalone entry). The new GEM-32 entry is `"STADT INNSBRUCK"` (all-caps, with "STADT" prefix). These are DISTINCT entries. Do NOT remove either one.

Before adding GEM-32, verify no entry named exactly "STADT INNSBRUCK" already exists. Use Python or a direct JSON scan to check.

## Line-break rules (QUAL-02)

The `%` character signals a visual line-break in the UI renderer. Use it for names that would be too long to display on a single line (>16 characters without the `%`).

Apply `%` as follows for the new entries:
- Names ≤16 chars: no `%` needed
- Names >16 chars: insert `%` at the most natural word boundary (usually between words)

**Bezirke — none need `%` (all ≤16 chars):**
- "BEZIRK IMST" (11)
- "BEZIRK INNSBRUCK-LAND" → 21 chars → use `"BEZIRK % INNSBRUCK-LAND"` (consistent with existing Steiermark pattern `"BEZIRK % DEUTSCHLANDSBERG"`)
- "BEZIRK KITZBÜHEL" (16) → borderline, but existing similar entries like "BEZIRK PINZGAU" (14) have no %; use `"BEZIRK KITZBÜHEL"` (no %)
- "BEZIRK KUFSTEIN" (15) → no %
- "BEZIRK LANDECK" (14) → no %
- "BEZIRK LIENZ" (12) → no %
- "BEZIRK REUTTE" (13) → no %
- "BEZIRK SCHWAZ" (13) → no %

**Gemeinden needing `%` (names >16 chars total):**
- GEM-05 "BREITENBACH AM INN" (18) → `"BREITENBACH % AM INN"`
- GEM-08 "EBEN AM ACHENSEE" (16) → exactly 16, no % needed
- GEM-15 "HOPFGARTEN IM BRIXENTAL" (23) → `"HOPFGARTEN IM % BRIXENTAL"`
- GEM-17 "ISELBERG-STRONACH" (17) → `"ISELBERG-% STRONACH"`
- GEM-28 "ST. JOHANN IN TIROL" (19) → `"ST. JOHANN % IN TIROL"`
- GEM-41 "WEISSENBACH AM LECH" (19) → `"WEISSENBACH % AM LECH"`

All other Gemeinden are ≤16 chars (check individually; when in doubt leave without % if ≤16).

**Organisationen needing `%`:**
- ORG-04 "GRÜNE GENERATION PLUS" (21) → `"GRÜNE GENERATION% PLUS"`
- ORG-06 "GRÜNE BÄUERINNEN UND BAUERN" (27) → `"GRÜNE BÄUERINNEN% UND BAUERN"`
- ORG-07 "GRÜNE IN DER AK" (15) → no % needed

## Complete lists of new entries

### 8 Tiroler Bezirke (add to "Bezirke")
Use the `%` patterns from the rules above:
1. `"BEZIRK IMST"`
2. `"BEZIRK % INNSBRUCK-LAND"`
3. `"BEZIRK KITZBÜHEL"`
4. `"BEZIRK KUFSTEIN"`
5. `"BEZIRK LANDECK"`
6. `"BEZIRK LIENZ"`
7. `"BEZIRK REUTTE"`
8. `"BEZIRK SCHWAZ"`

### 43 Tiroler Gemeinden (add to "Gemeinden")
Use the `%` patterns from the rules above:
1. `"ABSAM"` (GEM-01)
2. `"ALDRANS"` (GEM-02)
3. `"AXAMS"` (GEM-03)
4. `"BAD HÄRING"` (GEM-04)
5. `"BREITENBACH % AM INN"` (GEM-05)
6. `"BREITENWANG"` (GEM-06)
7. `"BRIXLEGG"` (GEM-07)
8. `"EBEN AM ACHENSEE"` (GEM-08)
9. `"EHRWALD"` (GEM-09)
10. `"FIEBERBRUNN"` (GEM-10)
11. `"REUTTE"` (GEM-11)
12. `"GÖTZENS"` (GEM-12)
13. `"HAIMING"` (GEM-13)
14. `"HALL IN TIROL"` (GEM-14)
15. `"HOPFGARTEN IM % BRIXENTAL"` (GEM-15)
16. `"INZING"` (GEM-16)
17. `"ISELBERG-% STRONACH"` (GEM-17)
18. `"JENBACH"` (GEM-18)
19. `"KIRCHBICHL"` (GEM-19)
20. `"KÖSSEN"` (GEM-20)
21. `"KRAMSACH"` (GEM-21)
22. `"LANS"` (GEM-22)
23. `"MILS"` (GEM-23)
24. `"MUTTERS"` (GEM-24)
25. `"NATTERS"` (GEM-25)
26. `"ZILLERTAL"` (GEM-26)
27. `"SISTRANS"` (GEM-27)
28. `"ST. JOHANN % IN TIROL"` (GEM-28)
29. `"STADT IMST"` (GEM-29)
30. `"STADT KITZBÜHEL"` (GEM-30)
31. `"STADT KUFSTEIN"` (GEM-31)
32. `"STADT INNSBRUCK"` (GEM-32)
33. `"STADT LANDECK"` (GEM-33)
34. `"STADT LIENZ"` (GEM-34)
35. `"STADT SCHWAZ"` (GEM-35)
36. `"TELFS"` (GEM-36)
37. `"TERFENS"` (GEM-37)
38. `"THAUR"` (GEM-38)
39. `"TRINS"` (GEM-39)
40. `"VÖLS"` (GEM-40)
41. `"WEISSENBACH % AM LECH"` (GEM-41)
42. `"WÖRGL"` (GEM-42)
43. `"ZIRL"` (GEM-43)

### 7 Organisationen (add to "Andere")
1. `"GRÜNE FRAUEN"` (ORG-01)
2. `"GRÜNE JUGEND"` (ORG-02)
3. `"GRAS"` (ORG-03)
4. `"GRÜNE GENERATION% PLUS"` (ORG-04)
5. `"GRÜNE WIRTSCHAFT"` (ORG-05)
6. `"GRÜNE BÄUERINNEN% UND BAUERN"` (ORG-06)
7. `"GRÜNE IN DER AK"` (ORG-07)

## Alphabetical sort rules

Sort is locale-aware German alphabetical order (Ä after A, Ö after O, Ü after U for sorting purposes). When sorting, treat the `%` character as a space (i.e. sort by the full name ignoring `%`). Sort within each category independently. Do NOT sort `Klubs` or `Gebiete` (not in requirements).

Required categories to sort: `Bundesländer`, `Bezirke`, `Gemeinden`, `Andere`.

For `Bundesländer`, the current entries are:
- "ZELENI/ZÖLDEK/SELENE%BURGENLAND", "SALZBURG", "10. BUNDESLAND", "STEIERMARK", "Vorarlberg", "Kärnten", "Tirol", "Wien"

Sorted (digits before letters in German collation; treat "10." as starting with "1"):
1. "10. BUNDESLAND"
2. "Kärnten"
3. "SALZBURG"
4. "STEIERMARK"
5. "Tirol"
6. "Vorarlberg"
7. "Wien"
8. "ZELENI/ZÖLDEK/SELENE%BURGENLAND"

(Use Python's `locale`-aware sort or a simple key function that lowercases and replaces `%` with ` ` for comparison. Standard Python sort with `key=lambda x: x.replace('%', ' ').lower()` is acceptable and gives consistent results.)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Check for pre-existing duplicates in index.json</name>
  <files>resources/images/logos/index.json</files>
  <action>
Before modifying anything, run a duplicate-check script to establish a clean baseline. Execute the following Python one-liner from the repo root:

```bash
python3 - <<'EOF'
import json
with open('resources/images/logos/index.json', encoding='utf-8') as f:
    data = json.load(f)
all_entries = []
for cat, entries in data.items():
    for e in entries:
        all_entries.append((cat, e))
seen = {}
for cat, e in all_entries:
    key = e.strip()
    if key in seen:
        print(f"DUPLICATE: '{key}' in {seen[key]} AND {cat}")
    else:
        seen[key] = cat

# Check specifically for STAD INNSBRUCK
for cat, entries in data.items():
    if any("STADT INNSBRUCK" in e for e in entries):
        print(f"WARNING: 'STADT INNSBRUCK' already exists in {cat}")
    if any(e == "Innsbruck" for e in entries):
        print(f"INFO: 'Innsbruck' (mixed-case) exists in {cat} — this is the existing entry, keep it")
print("Duplicate check complete.")
EOF
```

Review the output. If "STADT INNSBRUCK" already exists, skip GEM-32. Otherwise proceed. The mixed-case "Innsbruck" entry MUST be kept — it is NOT a duplicate of "STADT INNSBRUCK".
  </action>
  <verify>Script runs without Python errors (non-zero exit would indicate malformed JSON)</verify>
  <done>Duplicate check output reviewed; confirmed whether STADT INNSBRUCK pre-exists; confirmed "Innsbruck" exists and is distinct</done>
</task>

<task type="auto">
  <name>Task 2: Add all new Tiroler entries to index.json, sort all required categories</name>
  <files>resources/images/logos/index.json</files>
  <action>
Write and run a Python script that performs ALL modifications atomically in a single pass, then writes the result back. Do NOT edit the JSON manually with text-editor tools — use Python to ensure correct JSON formatting and avoid off-by-one errors on 467+ entries.

Script logic (implement this exactly):

```python
import json

INPUT = 'resources/images/logos/index.json'

with open(INPUT, encoding='utf-8') as f:
    data = json.load(f)

# --- New Bezirke entries ---
new_bezirke = [
    "BEZIRK IMST",
    "BEZIRK % INNSBRUCK-LAND",
    "BEZIRK KITZBÜHEL",
    "BEZIRK KUFSTEIN",
    "BEZIRK LANDECK",
    "BEZIRK LIENZ",
    "BEZIRK REUTTE",
    "BEZIRK SCHWAZ",
]

# --- New Gemeinden entries ---
new_gemeinden = [
    "ABSAM",
    "ALDRANS",
    "AXAMS",
    "BAD HÄRING",
    "BREITENBACH % AM INN",
    "BREITENWANG",
    "BRIXLEGG",
    "EBEN AM ACHENSEE",
    "EHRWALD",
    "FIEBERBRUNN",
    "REUTTE",
    "GÖTZENS",
    "HAIMING",
    "HALL IN TIROL",
    "HOPFGARTEN IM % BRIXENTAL",
    "INZING",
    "ISELBERG-% STRONACH",
    "JENBACH",
    "KIRCHBICHL",
    "KÖSSEN",
    "KRAMSACH",
    "LANS",
    "MILS",
    "MUTTERS",
    "NATTERS",
    "ZILLERTAL",
    "SISTRANS",
    "ST. JOHANN % IN TIROL",
    "STADT IMST",
    "STADT KITZBÜHEL",
    "STADT KUFSTEIN",
    "STADT INNSBRUCK",
    "STADT LANDECK",
    "STADT LIENZ",
    "STADT SCHWAZ",
    "TELFS",
    "TERFENS",
    "THAUR",
    "TRINS",
    "VÖLS",
    "WEISSENBACH % AM LECH",
    "WÖRGL",
    "ZIRL",
]

# --- New Andere (Organisationen) entries ---
new_andere = [
    "GRÜNE FRAUEN",
    "GRÜNE JUGEND",
    "GRAS",
    "GRÜNE GENERATION% PLUS",
    "GRÜNE WIRTSCHAFT",
    "GRÜNE BÄUERINNEN% UND BAUERN",
    "GRÜNE IN DER AK",
]

def sort_key(s):
    """Sort key: strip %, collapse spaces, lowercase for German-ish collation."""
    return s.replace('%', ' ').replace('  ', ' ').strip().lower()

def dedupe_add(existing, additions):
    """Add entries from additions that don't already exist (exact string match), return merged list."""
    existing_set = set(existing)
    result = list(existing)
    for entry in additions:
        if entry not in existing_set:
            result.append(entry)
            existing_set.add(entry)
    return result

# Add new entries (deduplication guards against re-running)
data['Bezirke'] = dedupe_add(data['Bezirke'], new_bezirke)
data['Gemeinden'] = dedupe_add(data['Gemeinden'], new_gemeinden)
data['Andere'] = dedupe_add(data['Andere'], new_andere)

# Sort required categories
for cat in ['Bundesländer', 'Bezirke', 'Gemeinden', 'Andere']:
    data[cat] = sorted(data[cat], key=sort_key)

# Write back with consistent formatting
with open(INPUT, 'w', encoding='utf-8') as f:
    json.dump(data, f, ensure_ascii=False, indent=2)
    f.write('\n')  # trailing newline

print("Done. Counts:")
for cat, entries in data.items():
    print(f"  {cat}: {len(entries)} entries")
```

Run the script from the repository root:
```bash
python3 /tmp/update_index.py
```
(Save the script to /tmp first, then run it.)

After running, verify the JSON is valid:
```bash
python3 -c "import json; json.load(open('resources/images/logos/index.json', encoding='utf-8')); print('JSON valid')"
```

Spot-check the sort order for Bezirke, Gemeinden, Andere, and Bundesländer by printing the first 10 entries of each:
```bash
python3 -c "
import json
data = json.load(open('resources/images/logos/index.json', encoding='utf-8'))
for cat in ['Bundesländer', 'Bezirke', 'Gemeinden', 'Andere']:
    print(f'--- {cat} (first 10) ---')
    for e in data[cat][:10]: print(' ', repr(e))
"
```

Verify all 8 Bezirke exist:
```bash
python3 -c "
import json
data = json.load(open('resources/images/logos/index.json', encoding='utf-8'))
required = ['BEZIRK IMST','BEZIRK % INNSBRUCK-LAND','BEZIRK KITZBÜHEL','BEZIRK KUFSTEIN','BEZIRK LANDECK','BEZIRK LIENZ','BEZIRK REUTTE','BEZIRK SCHWAZ']
for r in required:
    found = r in data['Bezirke']
    print(f'  {\"OK\" if found else \"MISSING\"}: {r}')
"
```

Verify all 43 Gemeinden exist:
```bash
python3 -c "
import json
data = json.load(open('resources/images/logos/index.json', encoding='utf-8'))
required = ['ABSAM','ALDRANS','AXAMS','BAD HÄRING','BREITENBACH % AM INN','BREITENWANG','BRIXLEGG','EBEN AM ACHENSEE','EHRWALD','FIEBERBRUNN','REUTTE','GÖTZENS','HAIMING','HALL IN TIROL','HOPFGARTEN IM % BRIXENTAL','INZING','ISELBERG-% STRONACH','JENBACH','KIRCHBICHL','KÖSSEN','KRAMSACH','LANS','MILS','MUTTERS','NATTERS','ZILLERTAL','SISTRANS','ST. JOHANN % IN TIROL','STADT IMST','STADT KITZBÜHEL','STADT KUFSTEIN','STADT INNSBRUCK','STADT LANDECK','STADT LIENZ','STADT SCHWAZ','TELFS','TERFENS','THAUR','TRINS','VÖLS','WEISSENBACH % AM LECH','WÖRGL','ZIRL']
for r in required:
    found = r in data['Gemeinden']
    print(f'  {\"OK\" if found else \"MISSING\"}: {r}')
"
```

Verify all 7 Organisationen exist in Andere:
```bash
python3 -c "
import json
data = json.load(open('resources/images/logos/index.json', encoding='utf-8'))
required = ['GRÜNE FRAUEN','GRÜNE JUGEND','GRAS','GRÜNE GENERATION% PLUS','GRÜNE WIRTSCHAFT','GRÜNE BÄUERINNEN% UND BAUERN','GRÜNE IN DER AK']
for r in required:
    found = r in data['Andere']
    print(f'  {\"OK\" if found else \"MISSING\"}: {r}')
"
```

Verify "Innsbruck" (existing mixed-case entry) is still present in Gemeinden:
```bash
python3 -c "
import json
data = json.load(open('resources/images/logos/index.json', encoding='utf-8'))
print('Innsbruck present:', 'Innsbruck' in data['Gemeinden'])
print('STADT INNSBRUCK present:', 'STADT INNSBRUCK' in data['Gemeinden'])
"
```

Verify Bezirke is sorted (spot-check: BEZIRK IMST should appear before BEZIRK KUFSTEIN):
```bash
python3 -c "
import json
data = json.load(open('resources/images/logos/index.json', encoding='utf-8'))
b = data['Bezirke']
for i, (a, bb) in enumerate(zip(b, b[1:])):
    key_a = a.replace('%',' ').strip().lower()
    key_b = bb.replace('%',' ').strip().lower()
    if key_a > key_b:
        print(f'SORT ERROR at position {i}: {repr(a)} > {repr(bb)}')
        break
else:
    print('Bezirke: sort OK')
"
```

Run the same sort-order check for Gemeinden and Andere:
```bash
python3 -c "
import json
data = json.load(open('resources/images/logos/index.json', encoding='utf-8'))
for cat in ['Gemeinden', 'Andere', 'Bundesländer']:
    lst = data[cat]
    ok = True
    for i, (a, b) in enumerate(zip(lst, lst[1:])):
        if a.replace('%',' ').strip().lower() > b.replace('%',' ').strip().lower():
            print(f'{cat} SORT ERROR at {i}: {repr(a)} > {repr(b)}')
            ok = False
            break
    if ok:
        print(f'{cat}: sort OK')
"
```
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('resources/images/logos/index.json',encoding='utf-8')); assert 'BEZIRK IMST' in d['Bezirke'], 'missing'; assert 'ABSAM' in d['Gemeinden'], 'missing'; assert 'GRÜNE JUGEND' in d['Andere'], 'missing'; assert 'Innsbruck' in d['Gemeinden'], 'original Innsbruck removed!'; print('All spot-checks passed')"</automated>
  </verify>
  <done>
- index.json is valid JSON
- All 8 Tiroler Bezirke entries are present in the Bezirke category
- All 43 Tiroler Gemeinden entries are present in the Gemeinden category
- All 7 Organisationen entries are present in the Andere category
- The existing "Innsbruck" (mixed-case) entry is still present alongside "STADT INNSBRUCK"
- Bezirke, Gemeinden, Andere, Bundesländer are all in alphabetical order
- Names >16 chars use `%` line-break at natural word boundaries
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify production build still works</name>
  <files>resources/images/logos/index.json</files>
  <action>
Run embed_logos.py against the actual index.html to confirm the production build pipeline still works end-to-end with the updated index.json.

```bash
python3 embed_logos.py index.html /tmp/test-output.html
echo "Exit code: $?"
```

The script reads `resources/images/logos/index.json` via `json.load()` and embeds it as inline JavaScript into the HTML. If the JSON is malformed or has encoding issues, the script exits non-zero and prints an error to stderr.

Expected output:
```
Successfully embedded logo data into /tmp/test-output.html
  Original HTML: X bytes
  Logo JSON: Y bytes
  Final HTML: Z bytes (+N bytes)
Exit code: 0
```

If the exit code is non-zero or the "Successfully embedded" line is missing, the JSON is broken. Fix the JSON before proceeding.

Additionally, verify the embedded output contains the new Tiroler entries:
```bash
python3 -c "
content = open('/tmp/test-output.html', encoding='utf-8').read()
checks = ['BEZIRK IMST', 'ABSAM', 'GRÜNE JUGEND', 'STADT INNSBRUCK']
for c in checks:
    found = c in content
    print(f'  {\"OK\" if found else \"MISSING\"}: {c} in build output')
"
```
  </action>
  <verify>
    <automated>python3 embed_logos.py index.html /tmp/test-output.html && python3 -c "c=open('/tmp/test-output.html',encoding='utf-8').read(); assert 'BEZIRK IMST' in c; assert 'ABSAM' in c; assert 'GRÜNE JUGEND' in c; print('Build verification passed')"</automated>
  </verify>
  <done>
- `embed_logos.py` exits with code 0
- Output HTML contains "Successfully embedded logo data"
- Build output contains BEZIRK IMST, ABSAM, GRÜNE JUGEND, STADT INNSBRUCK
- No errors printed to stderr
  </done>
</task>

</tasks>

<verification>
Run the full verification suite:

```bash
# 1. JSON validity
python3 -c "import json; json.load(open('resources/images/logos/index.json', encoding='utf-8')); print('JSON valid')"

# 2. Count all entries (should be ~467 + 58 new = ~525 or more if no duplicates removed)
python3 -c "
import json
data = json.load(open('resources/images/logos/index.json', encoding='utf-8'))
total = sum(len(v) for v in data.values())
print(f'Total entries: {total}')
for cat, entries in data.items():
    print(f'  {cat}: {len(entries)}')
"

# 3. All 62 requirement entries present
python3 -c "
import json
data = json.load(open('resources/images/logos/index.json', encoding='utf-8'))
required_bezirke = ['BEZIRK IMST','BEZIRK % INNSBRUCK-LAND','BEZIRK KITZBÜHEL','BEZIRK KUFSTEIN','BEZIRK LANDECK','BEZIRK LIENZ','BEZIRK REUTTE','BEZIRK SCHWAZ']
required_gemeinden = ['ABSAM','ALDRANS','AXAMS','BAD HÄRING','BREITENBACH % AM INN','BREITENWANG','BRIXLEGG','EBEN AM ACHENSEE','EHRWALD','FIEBERBRUNN','REUTTE','GÖTZENS','HAIMING','HALL IN TIROL','HOPFGARTEN IM % BRIXENTAL','INZING','ISELBERG-% STRONACH','JENBACH','KIRCHBICHL','KÖSSEN','KRAMSACH','LANS','MILS','MUTTERS','NATTERS','ZILLERTAL','SISTRANS','ST. JOHANN % IN TIROL','STADT IMST','STADT KITZBÜHEL','STADT KUFSTEIN','STADT INNSBRUCK','STADT LANDECK','STADT LIENZ','STADT SCHWAZ','TELFS','TERFENS','THAUR','TRINS','VÖLS','WEISSENBACH % AM LECH','WÖRGL','ZIRL']
required_andere = ['GRÜNE FRAUEN','GRÜNE JUGEND','GRAS','GRÜNE GENERATION% PLUS','GRÜNE WIRTSCHAFT','GRÜNE BÄUERINNEN% UND BAUERN','GRÜNE IN DER AK']
all_ok = True
for r in required_bezirke:
    if r not in data['Bezirke']:
        print(f'MISSING from Bezirke: {r}'); all_ok = False
for r in required_gemeinden:
    if r not in data['Gemeinden']:
        print(f'MISSING from Gemeinden: {r}'); all_ok = False
for r in required_andere:
    if r not in data['Andere']:
        print(f'MISSING from Andere: {r}'); all_ok = False
if all_ok:
    print('All 58 new entries present')
"

# 4. Production build works
python3 embed_logos.py index.html /tmp/verify-output.html && echo "BUILD OK" || echo "BUILD FAILED"
```
</verification>

<success_criteria>
- `resources/images/logos/index.json` is valid JSON
- All 8 Tiroler Bezirke entries present in `Bezirke` category
- All 43 Tiroler Gemeinden entries present in `Gemeinden` category
- All 7 Organisationen entries present in `Andere` category
- `Bezirke`, `Gemeinden`, `Andere`, `Bundesländer` are each alphabetically sorted
- No duplicate entries (verified by the deduplication check in Task 1 and the idempotent script in Task 2)
- The existing mixed-case "Innsbruck" entry in `Gemeinden` is preserved alongside "STADT INNSBRUCK"
- `python3 embed_logos.py index.html /tmp/test-output.html` exits with code 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-tiroler-logos/01-01-SUMMARY.md` following the template at `@/opt/claude-config/get-shit-done/templates/summary.md`.
</output>
